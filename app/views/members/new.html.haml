%p.select-task
  = t("labels.new_user")
.row
  .span5.offset2
    = simple_form_for member, url: members_path, html: { class: "form-horizontal", method: :post } do |f|
      = f.input :email
      = f.input :password
      = f.input :password_confirmation
      = f.input :first_name
      = f.input :last_name
      - if current_user.role?(:admin)
        = f.input :role, as: :select, collection: User::ROLES.invert, include_blank: t("labels.select")
      .control-group
        .controls.right
          = link_to t("labels.back"), members_url, :class => "button alternative"
          = link_to t("labels.save"), "javascript:;", :class => "button submit-this"
